// Seed-building CLI. Fills out in Checkpoint 2: reads the committed GeoJSON and
// prefecture-mascots.json from /data, loads them into PostGIS, and computes the
// stored ST_PointOnSurface label points.
Console.WriteLine("YuruChara.Ingestion — not implemented yet (Checkpoint 2).");
return 0;
